{
  "name": "javify-control-root",
  "version": "1.0.0",
  "description": "Root configuration for Javify Control unified Vercel deployment",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mv frontend/dist dist"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.5"
  }
}
